{
	/*
	 * Simple use case which creates a thread that run 20ms then sleep 80ms
	 * until the use case is stopped with Ctrl+C // false
	 */
	"tasks" : {
		"thread0" : {
			"loop" : -1,
			"run" :   2000,
			"sleep" : 16000
		}
	},
	"global" : {
		"duration" : 2,
		"calibration" : 42,
		"default_policy" : "SCHED_OTHER",
		"pi_enabled" : false,
		"lock_pages" : false,
		"logdir" : "./",
		"log_basename" : "rt-app1",
		"ftrace" : "main,task,loop,event",
		"gnuplot" : true,
	}
}
